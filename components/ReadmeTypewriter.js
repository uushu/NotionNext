/**
 * Claude 首页 README 动画占位组件。
 *
 * 当前关闭 README 动画，确保内容随服务端 HTML 首屏直接完整显示：
 * - 不等待 React hydration
 * - 不遍历、清空或重写 README 文本节点
 * - 不注入 clip-path、光标或逐字动画样式
 * - 不影响灵宠、贡献图和首页布局
 */
export default function ReadmeTypewriter() {
  return null
}
